name: Publish Skills

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install CLI tools
        run: |
          npm install -g clawhub
          npm install -g @smithery/cli

      - name: Publish skills to ClawHub
        run: |
          # Skills in this list are internal dev tools and must not be published.
          SKIP_DIRS="eval-run-toolbelt"
          failed=0
          for dir in */; do
            dir="${dir%/}"
            if echo "$SKIP_DIRS" | grep -qw "$dir"; then
              echo "Skipping $dir (internal — not published to ClawHub)"
              continue
            fi
            if [ -f "$dir/SKILL.md" ]; then
              echo "Publishing $dir..."
              if (cd "$dir" && clawhub publish); then
                echo "✓ $dir published to ClawHub"
              else
                echo "✗ $dir failed on ClawHub"
                failed=1
              fi
            fi
          done
          exit $failed

      - name: Publish MCP server to Smithery
        run: smithery mcp publish "https://mcp.toolbelt.ai/mcp" -n toolbeltai/toolbelt
